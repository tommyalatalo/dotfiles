#================================================
#                    Consul
#================================================

alias cm='consul members'

kvimp () {
    consul kv import "$(cat $1 | jq '. | to_entries | . | map({key:.value.key, value:(.value.value| @base64)})')"
}
